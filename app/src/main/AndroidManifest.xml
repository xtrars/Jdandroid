<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Idle phases (Click'n'Load listening, waiting for Wi-Fi) run as specialUse
         so they do not consume the 6-hour dataSync budget of Android 15. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- No backups: accounts and the download list must not leave the device,
         and the Keystore key would not be restored anyway. -->
    <application
        android:name=".JdApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="tiramisu"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jdandroid">

        <!-- adjustResize: below Android 11 the IME insets (imePadding) are only
             reported in this mode; with edge-to-edge on newer versions it has
             no effect. -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Links shared from other apps -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <!-- DLC containers by MIME type: Android does not know .dlc and
                 hands such files over as application/octet-stream with a
                 content:// URI without extension. The app checks the content. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>
            <!-- DLC containers by extension. pathAdvancedPattern, because ".*"
                 in pathPattern does not backtrack and "Show.S01.dlc" would not
                 match. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:host="*" />
                <data android:pathAdvancedPattern=".*\\.[dD][lL][cC]" />
                <!-- Below Android 12 only pathPattern applies: one pattern per
                     dot in the file name. -->
                <data android:pathPattern=".*\\.dlc" />
                <data android:pathPattern=".*\\..*\\.dlc" />
                <data android:pathPattern=".*\\..*\\..*\\.dlc" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.dlc" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\.dlc" />
            </intent-filter>
            <!-- DLC shared from file managers -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>
        </activity>

        <service
            android:name=".engine.DownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync|specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Click'n'Load-Empfang auf localhost:9666 und Warten auf WLAN zwischen Downloads" />
        </service>

        <!-- A dataSync service may not start from the background after boot
             (Android 15); the receiver repairs the queue and posts a reminder. -->
        <receiver
            android:name=".engine.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
