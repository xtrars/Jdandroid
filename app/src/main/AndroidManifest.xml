<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Leerlauf (Click'n'Load lauscht, Warten auf WLAN) laeuft als specialUse,
         damit das 6-Stunden-Kontingent fuer dataSync (Android 15) nicht
         verbraucht wird, waehrend nichts geladen wird. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
      allowBackup="false": Konten (auch verschluesselt) und Download-Liste
      gehoeren nicht in Cloud-Backups. Ein Backup wuerde zudem den Keystore-
      Schluessel nicht mitnehmen und die Konten waeren nach Wiederherstellung
      ohnehin unbrauchbar.
    -->
    <application
        android:name=".JdApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="tiramisu"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jdandroid">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Links aus anderen Apps teilen (z.B. Browser -> Teilen) -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <!--
              DLC-Container oeffnen, Weg 1: nach MIME-Typ. Android kennt die
              Endung .dlc nicht und meldet solche Dateien als
              application/octet-stream - so liefern Downloads-App, Chrome und
              Dateimanager sie (content://-URI ohne Endung im Pfad). Ob es
              wirklich ein DLC ist, prueft die App am Inhalt.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>
            <!--
              Weg 2: nach Dateiendung (file:// bzw. content:// mit sichtbarem
              Pfad). pathAdvancedPattern statt pathPattern, weil ".*" bei
              pathPattern nicht zuruecksetzt und "Serie.S01.dlc" sonst nicht
              erkannt wuerde.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:host="*" />
                <data android:pathAdvancedPattern=".*\\.[dD][lL][cC]" />
                <!-- pathAdvancedPattern gilt erst ab Android 12; darunter greifen
                     diese pathPattern-Varianten (".*" setzt dort nicht zurueck,
                     daher je Punkt im Namen ein eigenes Muster). -->
                <data android:pathPattern=".*\\.dlc" />
                <data android:pathPattern=".*\\..*\\.dlc" />
                <data android:pathPattern=".*\\..*\\..*\\.dlc" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.dlc" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\.dlc" />
            </intent-filter>
            <!-- DLC per "Teilen" aus Dateimanagern -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>
        </activity>

        <service
            android:name=".engine.DownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync|specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Click'n'Load-Empfang auf localhost:9666 und Warten auf WLAN zwischen Downloads" />
        </service>

        <!--
          Nach einem Neustart darf ein dataSync-Dienst nicht direkt aus dem
          Hintergrund starten (Android 15). Der Receiver stellt die Warteschlange
          konsistent her und erinnert per Benachrichtigung an offene Downloads.
        -->
        <receiver
            android:name=".engine.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
